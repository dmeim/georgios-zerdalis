---
import BaseLayout from '../layouts/BaseLayout.astro';
import HomeExperience from '../components/react/HomeExperience';
import { getImage } from 'astro:assets';
import portrait from '../assets/portrait.jpeg';
import {
  meta,
  hero,
  currentChapter,
  appointments,
  shortBio,
  mentorQuote,
  pedagogy,
  endorsement,
  venues,
} from '../content/site';

const optimized = await getImage({
  src: portrait,
  width: 1920,
  format: 'webp',
  quality: 82,
});
---

<BaseLayout title="Georgios Zerdalis" description={meta.description}>
  <HomeExperience
    client:load
    portraitSrc={optimized.src}
    portraitWidth={optimized.attributes.width}
    portraitHeight={optimized.attributes.height}
    hero={hero}
    chapter={currentChapter}
    appointments={appointments}
    bio={shortBio}
    quote={mentorQuote}
    pedagogy={pedagogy}
    endorsement={endorsement}
    venues={venues}
  />
</BaseLayout>

<style is:global>
  /* Full-bleed home composition inside constrained .site-main */
  .site-main:has(.he) {
    width: 100%;
    max-width: none;
    margin-inline: 0;
    padding-block: 0;
  }
</style>
